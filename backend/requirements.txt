fastapi==0.115.*
uvicorn[standard]==0.30.*
httpx>=0.27
# Domain lib (private, not on PyPI). Installed straight from the public
# Seretos repo and pinned to the exact tag v0.2.4 for reproducible builds.
# The transitive lib-python-config git dependency is resolved automatically
# from its pyproject.
lib-python-projects @ git+https://github.com/Seretos/lib-python-projects@v0.2.4
lib-python-worktree @ git+https://github.com/Seretos/lib-python-worktree@v0.1.9
