<!-- This repo standardizes on AGENTS.md. Claude Code does not auto-load AGENTS.md (it reads
     CLAUDE.md), so this file imports it — keeping a single source of truth for every tool.
     This comment is stripped before injection and costs no context. -->
@AGENTS.md
